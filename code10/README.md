1. 任务介绍
本节课是Python编程的进阶篇，我们将不再满足于简单的单文件代码，而是开始学习如何像专业程序员一样构建更大、更复杂的系统。课程内容分为两大块：一是对已学过的基础语法进行“难度提高一点”的深入理解和灵活运用；二是引入“模块化编程”的思想，学习如何将代码拆分到不同文件中进行组织和管理。通过本节课，我们将完成3个由浅入深的任务，体验从“写代码”到“设计代码”的转变。
2. 任务演示
## 任务一：基础语法强化——文本数据清洗
编写一个Python程序，要求对一个包含噪声数据的字符串列表进行清洗和统计。
```python
# 原始数据
raw_data = ["  Alice, 85 ", "BOB, 92", "张魁元, 100", "david, 78", "Ella, 95", ""]
# 任务要求：
# 1. 去除每个有效数据项两端的空格，并将姓名首字母大写，其他字母小写（如 "alice" -> "Alice"）。
# 2. 将处理后的数据存储为一个字典，格式为 {'Alice': 85, 'Bob': 92, ...}，跳过空字符串。
# 3. 使用条件语句和循环，找出并打印成绩最高的学生姓名和成绩。
# 4. （提高点）使用列表推导式生成一个所有成绩的列表，并计算平均分。

# 提示：可以使用字符串的 strip(), title(), split() 方法。

```
## 任务二：模块化编程——创建和使用自己的模块
将任务一中“清洗数据”和“计算平均分”的功能，封装成独立的函数，并放到一个名为data_cleaner.py的模块中。
1. 创建一个新的Python文件 data_cleaner.py。
2. 在该文件中定义函数 clean_data(raw_list)，它接收原始数据列表，返回清洗后的字典。
3. 在该文件中定义函数 calculate_average(score_dict)，它接收成绩字典，返回平均分。
4. 创建一个新的主程序文件 main.py。
5. 在 main.py 中，通过 import data_cleaner 导入自己创建的模块。
6. 调用模块中的函数，完成与任务一相同的功能，并打印结果。
(此处可展示简单的模块导入示意图或代码结构图)
## 任务三：探索Python标准库
Python自带了丰富的标准库，就像一个个功能强大的工具包。请编写程序，使用random模块和statistics模块来增强任务二的结果。
1. 在 main.py 中，导入 random 和 statistics 模块。
2. 使用 random.choice() 函数，从任务二生成的字典的键（学生姓名）中，随机抽取一名“幸运学生”并打印。
3. 使用 statistics.mean() 函数，计算学生成绩的平均值，并与你自己编写的 calculate_average 函数的结果进行对比验证。

## stdio的配置方式
使用如下配置
```markdown
{
  "mcpServers": {
    "weather-server": {
      "command": "uv",
      "args": [
        "run",
        "python",
        "E:\\code\\GitWork\\gdcvi\\lesson\\code23\\mcp_stdio\\serve.py"
      ]
    }
  }
}

```

## sse的配置方式
1、先启动mcp服务
2、使用如下配置
3、注意端口要一致
```markdown
{
  "mcpServers": {
    "weather-server-sse": {
      "url": "http://127.0.0.1:8080/sse"
    }
  }
}


```